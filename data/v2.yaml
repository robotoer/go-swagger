definitions:
  RangeFilter:
    allOf:
    - $ref: '#/definitions/Filter'
    - properties:
        config:
          properties:
            gt:
              format: double
              type: number
            gte:
              format: double
              type: number
            lt:
              format: double
              type: number
            lte:
              format: double
              type: number
          type: object
      required:
      - config
      type: object
  Filter:
    discriminator: type
#    properties:
#      type:
#        type: string
#    required:
#    - type
#    type: object
  AndFilter:
    allOf:
    - $ref: '#/definitions/Filter'
    - properties:
        config:
          items:
            $ref: '#/definitions/Filter'
          type: array
      required:
      - config
      type: object

